import { Stack, Typography } from "@mui/material";

const LastPremiumSignals = () => {
  return (
    <Stack>
      <Typography variant="h3-bold" sx={{ mb: 3 }}>
        LATST PREMIUM SIGNALS
      </Typography>
    </Stack>
  );
};

export default LastPremiumSignals;
